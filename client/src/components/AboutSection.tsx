import { useRef } from 'react';\nimport { motion } from 'framer-motion';\nimport { useScrollAnimation } from '@/hooks/useScrollAnimation';\nimport { Code2, Zap, Users, Trophy } from 'lucide-react';\n\n/**\n * Interface para skill\n */\ninterface Skill {\n  name: string;\n  level: number; // 0-100\n  category: string;\n}\n\n/**\n * Skills por categoria\n */\nconst SKILLS: Skill[] = [\n  // Frontend\n  { name: 'React', level: 95, category: 'Frontend' },\n  { name: 'TypeScript', level: 90, category: 'Frontend' },\n  { name: 'TailwindCSS', level: 92, category: 'Frontend' },\n  { name: 'Framer Motion', level: 88, category: 'Frontend' },\n  { name: 'Next.js', level: 85, category: 'Frontend' },\n\n  // Backend\n  { name: 'Node.js', level: 90, category: 'Backend' },\n  { name: 'PostgreSQL', level: 85, category: 'Backend' },\n  { name: 'MongoDB', level: 82, category: 'Backend' },\n  { name: 'REST APIs', level: 92, category: 'Backend' },\n  { name: 'Docker', level: 80, category: 'Backend' },\n\n  // Tools\n  { name: 'Git', level: 95, category: 'Tools' },\n  { name: 'Figma', level: 85, category: 'Tools' },\n  { name: 'AWS', level: 78, category: 'Tools' },\n  { name: 'CI/CD', level: 82, category: 'Tools' },\n];\n\n/**\n * Componente SkillBar\n */\nfunction SkillBar({ skill, index }: { skill: Skill; index: number }) {\n  return (\n    <motion.div\n      initial={{ opacity: 0, x: -20 }}\n      whileInView={{ opacity: 1, x: 0 }}\n      transition={{ duration: 0.5, delay: index * 0.05 }}\n      viewport={{ once: true }}\n      key={skill.name}\n      className=\"space-y-2\"\n    >\n      <div className=\"flex items-center justify-between\">\n        <span className=\"text-sm font-medium\">{skill.name}</span>\n        <span className=\"text-xs text-muted-foreground\">{skill.level}%</span>\n      </div>\n      <div className=\"w-full h-2 bg-muted rounded-full overflow-hidden\">\n        <motion.div\n          className=\"h-full bg-gradient-accent rounded-full\"\n          initial={{ width: 0 }}\n          whileInView={{ width: `${skill.level}%` }}\n          transition={{ duration: 1, delay: index * 0.05, ease: 'easeOut' }}\n          viewport={{ once: true }}\n        />\n      </div>\n    </motion.div>\n  );\n}\n\n/**\n * Componente StatCard\n */\nfunction StatCard({\n  icon: Icon,\n  label,\n  value,\n  index,\n}: {\n  icon: React.ComponentType<{ className: string }>;\n  label: string;\n  value: string;\n  index: number;\n}) {\n  return (\n    <motion.div\n      initial={{ opacity: 0, y: 20 }}\n      whileInView={{ opacity: 1, y: 0 }}\n      transition={{ duration: 0.6, delay: index * 0.1 }}\n      viewport={{ once: true }}\n      className=\"bg-card p-6 rounded-lg border border-border text-center hover-lift\"\n    >\n      <Icon className=\"w-8 h-8 mx-auto mb-3 text-accent\" />\n      <p className=\"text-2xl font-bold font-display mb-1\">{value}</p>\n      <p className=\"text-sm text-muted-foreground\">{label}</p>\n    </motion.div>\n  );\n}\n\n/**\n * Componente AboutSection\n * Seção sobre o desenvolvedor com skills e estatísticas\n * Design: Minimalismo Sofisticado\n */\nexport default function AboutSection() {\n  const containerRef = useScrollAnimation();\n\n  const stats = [\n    { icon: Code2, label: 'Projetos Completos', value: '25+' },\n    { icon: Zap, label: 'Anos de Experiência', value: '5+' },\n    { icon: Users, label: 'Clientes Satisfeitos', value: '40+' },\n    { icon: Trophy, label: 'Prêmios e Reconhecimentos', value: '12' },\n  ];\n\n  const skillsByCategory = {\n    Frontend: SKILLS.filter((s) => s.category === 'Frontend'),\n    Backend: SKILLS.filter((s) => s.category === 'Backend'),\n    Tools: SKILLS.filter((s) => s.category === 'Tools'),\n  };\n\n  return (\n    <section id=\"about\" className=\"py-20 md:py-32 space-y-16\" ref={containerRef}>\n      <div className=\"container\">\n        {/* Section Header */}\n        <motion.div\n          initial={{ opacity: 0, y: 20 }}\n          whileInView={{ opacity: 1, y: 0 }}\n          transition={{ duration: 0.6 }}\n          viewport={{ once: true }}\n          className=\"text-center space-y-4 mb-16\"\n        >\n          <h2 className=\"font-display text-4xl md:text-5xl font-bold\">\n            Sobre Mim\n          </h2>\n          <p className=\"text-foreground/70 text-lg max-w-2xl mx-auto\">\n            Desenvolvedor full-stack com paixão por criar soluções digitais\n            inovadoras que combinam design elegante com funcionalidade robusta.\n          </p>\n        </motion.div>\n\n        {/* Stats Grid */}\n        <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-20\">\n          {stats.map((stat, index) => (\n            <StatCard key={stat.label} {...stat} index={index} />\n          ))}\n        </div>\n\n        {/* About Content */}\n        <div className=\"grid grid-cols-1 lg:grid-cols-2 gap-12 items-center mb-20\">\n          {/* Text */}\n          <motion.div\n            initial={{ opacity: 0, x: -30 }}\n            whileInView={{ opacity: 1, x: 0 }}\n            transition={{ duration: 0.8 }}\n            viewport={{ once: true }}\n            className=\"space-y-6\"\n          >\n            <p className=\"text-foreground/80 leading-relaxed\">\n              Sou um desenvolvedor apaixonado por tecnologia e design, com mais\n              de 5 anos de experiência criando aplicações web e mobile que\n              resolvem problemas reais. Minha jornada começou com curiosidade\n              sobre como as coisas funcionam, e evoluiu para uma carreira\n              dedicada à excelência técnica.\n            </p>\n            <p className=\"text-foreground/80 leading-relaxed\">\n              Especializo-me em criar experiências de usuário fluidas e\n              intuitivas, combinando React, TypeScript e TailwindCSS no\n              frontend com Node.js, PostgreSQL e arquiteturas escaláveis no\n              backend. Acredito que bom design e código limpo são fundamentais\n              para o sucesso de qualquer projeto.\n            </p>\n            <p className=\"text-foreground/80 leading-relaxed\">\n              Quando não estou codificando, gosto de contribuir para projetos\n              open-source, escrever sobre desenvolvimento web e explorar novas\n              tecnologias. Estou sempre buscando aprender e crescer como\n              profissional.\n            </p>\n          </motion.div>\n\n          {/* Image Placeholder */}\n          <motion.div\n            initial={{ opacity: 0, x: 30 }}\n            whileInView={{ opacity: 1, x: 0 }}\n            transition={{ duration: 0.8 }}\n            viewport={{ once: true }}\n            className=\"relative h-96 rounded-lg overflow-hidden bg-gradient-to-br from-accent/20 to-accent/5 border border-border flex items-center justify-center\"\n          >\n            <div className=\"text-center space-y-4\">\n              <div className=\"w-24 h-24 rounded-full bg-gradient-accent mx-auto flex items-center justify-center\">\n                <span className=\"text-4xl font-bold text-white\">👨‍💻</span>\n              </div>\n              <p className=\"text-muted-foreground\">Foto do desenvolvedor</p>\n            </div>\n          </motion.div>\n        </div>\n\n        {/* Skills */}\n        <div className=\"space-y-12\">\n          <h3 className=\"font-display text-2xl font-bold text-center\">\n            Habilidades Técnicas\n          </h3>\n\n          <div className=\"grid grid-cols-1 md:grid-cols-3 gap-8\">\n            {Object.entries(skillsByCategory).map(([category, skills]) => (\n              <motion.div\n                key={category}\n                initial={{ opacity: 0, y: 20 }}\n                whileInView={{ opacity: 1, y: 0 }}\n                transition={{ duration: 0.6 }}\n                viewport={{ once: true }}\n                className=\"bg-card p-6 rounded-lg border border-border space-y-4\"\n              >\n                <h4 className=\"font-display font-semibold text-lg\">\n                  {category}\n                </h4>\n                <div className=\"space-y-4\">\n                  {skills.map((skill, index) => (\n                    <SkillBar key={skill.name} skill={skill} index={index} />\n                  ))}\n                </div>\n              </motion.div>\n            ))}\n          </div>\n        </div>\n      </div>\n    </section>\n  );\n}\n
